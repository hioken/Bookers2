<div class="row">

<div class="col-xs-3">
  <h2>User info</h2>
    <%= attachment_image_tag @user, :profile_image, :fill, 50, 50, format: 'png', fallback: "no_image.png", class: "attachment user profile_image fallback pro_image" %>
  <table class="table">
    <tbody><tr></tr>
    <tr>
       <th>name</th>
       <th><%= @user.name %></th>
    </tr>
    <tr>
       <th>introduction</th>
       <th><%= @user.introduction %></th>
    </tr>
</tbody></table>


  <%= link_to "", home_path, class: "col-xs-12 btn btn-default glyphicon glyphicon-wrench" %>
</div>

    <h2>New book</h2>

<div class="col-xs-9">
    <h2>Book detail</h2>
    <table class="table">
      <tbody>
    <tr>
    　　<td><a href="root"><%= attachment_image_tag @user, :profile_image, fallback: "no_image.png", class: "attachment user profile_image fallback index_image" %></a></td>
      <td><a href="home_path"><%= @book.title %></a></td>
    <td><%= @book.body %></td>
    <% if @book.user == current_user %>
      <td><%= link_to "Edit", edit_book_path, class: "btn-sm btn-success edit_book_22742" %></td>
      <td><%= link_to "Destroy", destroy_path, class: "btn-sm btn-success Destroy_book_22742" %></td>
    <% end %>
    </tr>
    <a href="root"><%= @user.name %></a>
    </tbody>
  </table>
</div>
</div>